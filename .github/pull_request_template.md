<!-- Title format: short, imperative. e.g. "Add per-person budget filter" -->

## What

<!-- One or two sentences: what does this PR change? -->

## Why

<!--
REQUIRED: link the issue this PR closes. CI fails (`pr-link-check`) if
no `Closes #N` / `Fixes #N` / `Resolves #N` is found below.
If there's truly no related issue (rare — repo-meta only), add the
`no-issue` label to the PR instead.
-->

Closes #

## How it was tested

<!-- Check what applies, add a sentence or two on what you actually did. -->

- [ ] `npm run lint` clean
- [ ] `npx tsc --noEmit` clean
- [ ] `npm test` passes
- [ ] `npm run build` succeeds
- [ ] Manually tested in the browser

## Screenshots / notes

<!-- Optional. Drag images here for UI changes. -->

## Risk / rollout

<!-- Anything reviewers should look at carefully? Anything to do after merge (env vars, Netlify hook, etc.)? -->
